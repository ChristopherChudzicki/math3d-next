import React from "react";
// Google's G, from the Sign in with Google branding assets.
import googleG from "./google-g.svg";

const GoogleLogo: React.FC = () => (
  <img src={googleG} alt="" width={20} height={20} />
);

export default GoogleLogo;
