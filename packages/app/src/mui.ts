import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#1890ff",
    },
    secondary: {
      main: "#8c8c8c",
    },
    text: {
      primary: "#434343",
    },
  },
});

export { theme };
