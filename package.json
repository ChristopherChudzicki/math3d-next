{
  "name": "math3d-next",
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "yarn@3.3.1",
  "devDependencies": {
    "@math3d/eslint-config": "workspace:*",
    "prettier": "^2.8.1",
    "ts-node": "^10.9.1",
    "turbo": "^1.6.3",
    "typescript": "^4.9.4"
  },
  "scripts": {
    "lint": "turbo run lint",
    "test": "turbo run test",
    "build": "yarn workspace app build"
  }
}
